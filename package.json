# Pair Aura Web V6 线上部署版复盘

## 1. 项目源一致性检查

- 仍然保持“关系解释器”定位，不是重新做测评平台。
- 仍然采用手动输入 MBTI/SBTI 的轻量 MVP，不接 OCR/AI，不增加用户成本。
- 仍然输出关系类型、合拍指数、相似度、互补度、摩擦值、场景展开卡、出圈短句和分享海报。
- 仍然保留“仅供娱乐，不构成心理诊断、职业筛选或严肃关系决策建议”的边界。

## 2. 本次上线优化

### 2.1 二维码修正

V5 使用的 `qrcode@1.5.4/build/qrcode.min.js` CDN 路径在部分 CDN 环境下可能不可用。V6 改成懒加载 ESM：

- `https://cdn.jsdelivr.net/npm/qrcode@1.5.4/+esm`
- `https://esm.sh/qrcode@1.5.4`

二维码生成失败时仍保留复制链接和复制文案兜底。

### 2.2 部署文件补全

新增：

- `_headers`：Netlify/Cloudflare 静态安全头。
- `_redirects`：为未来路径路由预留 SPA fallback。
- `netlify.toml`：Netlify 一键部署配置。
- `.nojekyll`：兼容 GitHub Pages。
- `robots.txt`：允许搜索引擎抓取。
- `DEPLOY_NOW.md`：上线步骤清单。
- `scripts/check-js.mjs`：抽取并检查内联 JS 语法。

### 2.3 分享链接优化

线上环境默认使用当前域名生成 `?r=` 报告链接。部署后无需手动填写公开域名；只有在本地预览或需要指定域名时才使用“公开域名设置”。

### 2.4 SEO / 社交预览基础信息

新增基础 meta：

- `application-name`
- `apple-mobile-web-app-title`
- `og:type`
- `twitter:card`
- `twitter:title`
- `twitter:description`

静态网页无法根据每份报告动态生成微信卡片标题；后续如果需要动态分享卡片，需要后端或边缘函数支持。

## 3. 质量检查

### 3.1 静态结构

- `index.html` 单文件仍可直接运行。
- CSS/JS 仍内嵌，避免用户只打开 HTML 时丢样式。
- `package.json`、`vercel.json`、`netlify.toml` 均已配置。

### 3.2 语法检查

命令：

```bash
npm run check:js
```

结果：通过。

### 3.3 功能逻辑检查

通过 Node VM 对核心纯函数做了三组测试：

1. MBTI × MBTI：ENFP 表达外放版 × ISTJ 计划强化版。
2. SBTI × MBTI：LOVE-R × INTJ 计划强化版。
3. SBTI × SBTI：CTRL × HHHH，并测试旧参数兼容。

检查点：

- 可生成关系标题。
- 可生成合拍指数。
- 可生成 2 张场景展开卡。
- 可生成 3 张文案卡。
- URL 编码和解码可还原报告输入。

### 3.4 数据库完整性

- MBTI 类型：16 个。
- MBTI 细分指标：9 个。
- SBTI 类型：27 个。

### 3.5 浏览器测试限制

本地容器环境对 Chromium 访问本地 HTTP 端口有管理员限制，`http://127.0.0.1` 被阻断；因此没有在容器里完成真实端到端浏览器点击测试。已用语法检查、核心函数测试、静态结构检查覆盖上线前主要风险。正式部署后，需要按 `DEPLOY_NOW.md` 做真实浏览器和微信环境测试。

## 4. 仍需上线后人工确认

- 线上首页是否正常打开。
- 移动端页面是否无横向溢出。
- 二维码是否能扫码打开同一报告。
- 复制链接是否能在无痕窗口还原报告。
- 微信内打开是否正常。
- 小红书/朋友圈保存海报是否清晰。

## 5. 下一步建议

上线后先做 30–50 个真实用户测试，重点记录：

- 完成率；
- 分享率；
- 用户觉得“准/不准”；
- 是否愿意发给 TA；
- 哪些文案被截图最多。

下一版优先做反馈按钮和 6 个轻量关系问题，而不是立刻接 OCR/AI。
